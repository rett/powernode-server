.box-body
  .pull-right
    = refresh_button(:node_instances) do
      = fa('refresh', 'refresh-button')
  #node_instances= render partial: 'node_instances', locals: { node_instances: @node.node_instances }
